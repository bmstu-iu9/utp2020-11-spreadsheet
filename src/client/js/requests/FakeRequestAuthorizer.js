/* eslint-disable no-unused-vars, class-methods-use-this */
import RequestAuthorizer from './RequestAuthorizer.js';

export default class FakeRequestAuthorizer extends RequestAuthorizer {
  setToken(token) {
  }

  authorize(request) {
  }
}
